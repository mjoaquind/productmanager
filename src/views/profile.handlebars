<div class="container mt-4 text-center">
    <h1>Perfil de Usuario (sin datos sensibles)</h1>
    <p>Nombre: {{user.full_name}}</p>
    <p>Email: {{user.email}}</p>
    <p>Edad: {{user.age}}</p>
    <a class="btn btn-primary" href="/api/session/logout">Salir</a>
</div>